diff --git a/ggml/include/ggml-backend.h b/ggml/include/ggml-backend.h
index cc3f8cd36..7b1175e9d 100644
--- a/ggml/include/ggml-backend.h
+++ b/ggml/include/ggml-backend.h
@@ -315,6 +315,11 @@ extern "C" {
     //
     typedef bool (*ggml_backend_sched_eval_callback)(struct ggml_tensor * t, bool ask, void * user_data);
 
+    // Optional pre-evaluation graph notification. The caller supplies the actual
+    // micro-batch token count (0 for vision); shares cb_eval_user_data. No compute
+    // is performed here. cb_eval observes completion using the existing scheduler.
+    typedef void (*ggml_backend_sched_graph_callback)(struct ggml_cgraph * graph, int64_t n_tokens, void * user_data);
+
     // Initialize a backend scheduler, backends with low index are given priority over backends with high index
     GGML_API ggml_backend_sched_t ggml_backend_sched_new(ggml_backend_t * backends, ggml_backend_buffer_type_t * bufts, int n_backends, size_t graph_size, bool parallel, bool op_offload);
     GGML_API void                 ggml_backend_sched_free(ggml_backend_sched_t sched);
diff --git a/include/llama.h b/include/llama.h
index ef7a012c4..bf72b96ee 100644
--- a/include/llama.h
+++ b/include/llama.h
@@ -383,6 +383,7 @@ extern "C" {
         uint32_t yarn_orig_ctx;    // YaRN original context size
         float    defrag_thold;     // [DEPRECATED] defragment the KV cache if holes/size > thold, <= 0 disabled (default)
 
+        ggml_backend_sched_graph_callback cb_eval_graph;
         ggml_backend_sched_eval_callback cb_eval;
         void * cb_eval_user_data;
 
diff --git a/src/llama-context.cpp b/src/llama-context.cpp
index 3cc27717e..06b9d7cef 100644
--- a/src/llama-context.cpp
+++ b/src/llama-context.cpp
@@ -137,6 +137,7 @@ llama_context::llama_context(
                                hparams.n_ctx_orig_yarn != 0 ? hparams.n_ctx_orig_yarn :
                                                               hparams.n_ctx_train;
 
+    cparams.cb_eval_graph     = params.cb_eval_graph;
     cparams.cb_eval           = params.cb_eval;
     cparams.cb_eval_user_data = params.cb_eval_user_data;
 
@@ -1391,6 +1392,9 @@ llm_graph_result * llama_context::process_ubatch(const llama_ubatch & ubatch, ll
         //LLAMA_LOG_INFO("graph set inputs time: %.3f ms\n", (ggml_time_us() - t_start_us)/1000.0);
     }
 
+    if (cparams.cb_eval_graph) {
+        cparams.cb_eval_graph(res->get_gf(), ubatch.n_tokens, cparams.cb_eval_user_data);
+    }
     const auto status = graph_compute(res->get_gf(), ubatch.n_tokens > 1);
     if (status != GGML_STATUS_SUCCESS) {
         LLAMA_LOG_ERROR("%s: failed to compute graph, compute status: %d\n", __func__, status);
@@ -3636,6 +3640,7 @@ llama_context_params llama_context_default_params() {
         /*.yarn_beta_slow              =*/ -1.0f,
         /*.yarn_orig_ctx               =*/ 0,
         /*.defrag_thold                =*/ -1.0f,
+        /*.cb_eval_graph               =*/ nullptr,
         /*.cb_eval                     =*/ nullptr,
         /*.cb_eval_user_data           =*/ nullptr,
         /*.type_k                      =*/ GGML_TYPE_F16,
diff --git a/src/llama-cparams.h b/src/llama-cparams.h
index b592de18c..cd6f697a9 100644
--- a/src/llama-cparams.h
+++ b/src/llama-cparams.h
@@ -60,6 +60,7 @@ struct llama_cparams {
     enum llama_rope_scaling_type rope_scaling_type;
     enum llama_pooling_type pooling_type;
 
+    ggml_backend_sched_graph_callback cb_eval_graph;
     ggml_backend_sched_eval_callback cb_eval;
     void * cb_eval_user_data;
 
diff --git a/tools/mtmd/clip.cpp b/tools/mtmd/clip.cpp
index 74f4e2b5a..932fc19f7 100644
--- a/tools/mtmd/clip.cpp
+++ b/tools/mtmd/clip.cpp
@@ -162,6 +162,8 @@ struct clip_ctx {
 
     int max_nodes = 8192;
     ggml_backend_sched_ptr sched;
+    ggml_backend_sched_graph_callback cb_eval_graph = nullptr;
+    void * cb_eval_user_data = nullptr;
     clip_flash_attn_type flash_attn_type = CLIP_FLASH_ATTN_TYPE_AUTO;
     bool is_allocated = false;
 
@@ -221,6 +223,8 @@ struct clip_ctx {
             ggml_backend_sched_new(backend_ptrs.data(), backend_buft.data(), backend_ptrs.size(), 8192, false, true)
         );
 
+        cb_eval_graph = ctx_params.cb_eval_graph;
+        cb_eval_user_data = ctx_params.cb_eval_user_data;
         if (ctx_params.cb_eval != nullptr) {
             ggml_backend_sched_set_eval_callback(sched.get(), ctx_params.cb_eval, ctx_params.cb_eval_user_data);
         }
@@ -5746,6 +5750,9 @@ bool clip_encode(struct clip_ctx * ctx, struct clip_encode_params * params) {
         }
     }
 
+    if (ctx->cb_eval_graph) {
+        ctx->cb_eval_graph(gf, 0, ctx->cb_eval_user_data);
+    }
     auto status = ggml_backend_sched_graph_compute(ctx->sched.get(), gf);
     if (status != GGML_STATUS_SUCCESS) {
         LOG_ERR("%s: ggml_backend_sched_graph_compute failed with error %d\n", __func__, status);
diff --git a/tools/mtmd/clip.h b/tools/mtmd/clip.h
index e07f25815..bbba5eea8 100644
--- a/tools/mtmd/clip.h
+++ b/tools/mtmd/clip.h
@@ -53,6 +53,7 @@ struct clip_context_params {
     int image_min_tokens;
     int image_max_tokens;
     bool warmup;
+    ggml_backend_sched_graph_callback cb_eval_graph;
     ggml_backend_sched_eval_callback cb_eval;
     void * cb_eval_user_data;
     bool no_alloc;
diff --git a/tools/mtmd/mtmd.cpp b/tools/mtmd/mtmd.cpp
index 00ecadcf4..713c365f3 100644
--- a/tools/mtmd/mtmd.cpp
+++ b/tools/mtmd/mtmd.cpp
@@ -467,6 +467,7 @@ mtmd_context_params mtmd_context_params_default() {
         /* warmup            */ true,
         /* image_min_tokens  */ -1,
         /* image_max_tokens  */ -1,
+        /* cb_eval_graph     */ nullptr,
         /* cb_eval           */ nullptr,
         /* cb_eval_user_data */ nullptr,
         /* batch_max_tokens  */ 1024,
@@ -572,6 +573,7 @@ struct mtmd_context {
             /* image_min_tokens  */ ctx_params.image_min_tokens,
             /* image_max_tokens  */ ctx_params.image_max_tokens,
             /* warmup            */ ctx_params.warmup,
+            /* cb_eval_graph     */ ctx_params.cb_eval_graph,
             /* cb_eval           */ ctx_params.cb_eval,
             /* cb_eval_user_data */ ctx_params.cb_eval_user_data,
             /* no_alloc          */ no_alloc,
diff --git a/tools/mtmd/mtmd.h b/tools/mtmd/mtmd.h
index c2de26eee..d45571c8d 100644
--- a/tools/mtmd/mtmd.h
+++ b/tools/mtmd/mtmd.h
@@ -109,6 +109,7 @@ struct mtmd_context_params {
     int image_max_tokens; // maximum number of tokens for image input (default: read from metadata)
 
     // callback function passed over to mtmd proper
+    ggml_backend_sched_graph_callback cb_eval_graph;
     ggml_backend_sched_eval_callback cb_eval;
     void * cb_eval_user_data;
 
